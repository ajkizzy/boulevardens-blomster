---
import Layout from '@/layouts/Layout.astro';
import ServicesPage from '@/components/ServicesPage.astro';
import { loadContent } from '@/lib/i18n';

const locale = 'en';
const services = loadContent<any>(locale, 'services');
---

<Layout title={services.heading} locale={locale}>
  <ServicesPage locale={locale} />
</Layout>
