---
import Layout from '@/layouts/Layout.astro';
import { loadContent } from '@/lib/i18n';

const locale = 'da';
const about = loadContent<any>(locale, 'about');
---

<Layout title={about.hero.heading} locale={locale}>
  <section class="px-4 pb-8 pt-10 sm:px-6 sm:pt-12 lg:px-8">
    <div class="container-narrow mx-auto">
      <div class="mx-auto max-w-4xl text-center">
        <div class="flex flex-col items-center">
          <p class="eyebrow">Vores rejse</p>
          <h1 class="mt-5 w-full max-w-[11.4ch] font-heading text-[2.55rem] leading-[0.92] tracking-[-0.05em] text-surface-ink sm:max-w-[9ch] sm:text-[4.7rem] lg:max-w-[8.5ch] lg:text-[5.6rem]">
            Om os
            <span class="inline-block align-top">—</span>
            <span class="block whitespace-nowrap">Historien bag</span>
            <span class="block italic">Boulevardens</span>
            <span class="block italic">Blomster</span>
          </h1>
        </div>

      </div>
    </div>
  </section>

  <section class="section-padding">
    <div class="container-narrow mx-auto">
      <div class="grid gap-8 lg:grid-cols-[0.92fr,1.08fr]">
        <div class="surface-panel relative min-h-[360px] overflow-hidden p-6 sm:p-8">
          <div class="absolute -right-10 top-8 h-40 w-40 rounded-full bg-secondary-100/70 blur-3xl"></div>
          <div class="absolute left-6 top-6 rounded-full bg-surface-raised px-4 py-2 text-[0.72rem] font-semibold uppercase tracking-[0.2em] text-primary-600 shadow-ambient">
            Håndbundne buketter
          </div>
          <div class="absolute bottom-6 left-6 right-12 top-20 rounded-[2rem] bg-gradient-to-br from-primary-100 via-surface-raised to-secondary-100 shadow-ambient"></div>
          <div class="absolute bottom-0 right-0 flex h-[66%] w-[62%] items-center justify-center rounded-[2rem] bg-gradient-to-br from-primary-500 to-primary-300 text-[6rem] text-white/20">
            ✿
          </div>
        </div>

        <div class="surface-panel p-6 sm:p-8">
          <p class="eyebrow">Om os</p>
          <h2 class="mt-5 font-heading text-4xl leading-tight text-surface-ink">{about.intro.heading}</h2>
          <div class="mt-6 space-y-5 text-base leading-relaxed text-surface-ink/70">
            {about.intro.text.split('\n\n').map((p: string) => <p>{p}</p>)}
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section-padding pt-0">
    <div class="container-narrow mx-auto">
      <div class="surface-panel px-6 py-8 sm:px-8 sm:py-10 lg:grid lg:grid-cols-[1.05fr,0.95fr] lg:gap-10">
        <div>
          <p class="eyebrow">Historien</p>
          <h2 class="mt-5 font-heading text-4xl leading-tight text-surface-ink">{about.story.heading}</h2>
          <div class="mt-6 space-y-5 text-base leading-relaxed text-surface-ink/70">
            {about.story.text.split('\n\n').map((p: string) => <p>{p}</p>)}
          </div>
        </div>
        <div class="mt-8 lg:mt-0">
          <div class="surface-panel min-h-[520px] overflow-hidden p-6">
            <div class="flex h-full min-h-[460px] flex-col items-center justify-center rounded-[1.75rem] border border-dashed border-primary-300/60 bg-gradient-to-br from-secondary-100 via-surface-raised to-accent-100 px-8 text-center">
              <div class="flex h-16 w-16 items-center justify-center rounded-full bg-surface-raised text-primary-500 shadow-ambient">
                <svg class="h-7 w-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2 1.586-1.586a2 2 0 012.828 0L20 14m-9-5h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                </svg>
              </div>
              <p class="mt-5 text-xs font-semibold uppercase tracking-[0.18em] text-primary-600">Billede kommer her</p>
              <p class="mt-3 max-w-xs text-sm leading-relaxed text-surface-ink/58">
                Plads til et fremtidigt billede af butikken eller blomsterarbejde.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>
