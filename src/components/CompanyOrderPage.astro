---
import Layout from '@/layouts/Layout.astro';
import Hero from '@/components/Hero.astro';
import type { Locale } from '@/lib/i18n';

interface Props {
  locale: Locale;
  order: any;
  paymentSuccess?: boolean;
  paymentCancelled?: boolean;
}

const {
  locale,
  order,
  paymentSuccess = false,
  paymentCancelled = false,
} = Astro.props;

const isEnglish = locale === 'en';
const deliveryTimeOptions = [
  '07:00',
  '07:15',
  '07:30',
  '07:45',
  '08:00',
  '08:15',
  '08:30',
  '08:45',
  '09:00',
  '16:00',
  '16:15',
  '16:30',
  '16:45',
  '17:00',
  '17:15',
  '17:30',
  '17:45',
  '18:00',
];

const uiText = isEnglish
  ? {
      stepOneTitle: 'Choose bouquets',
      stepOneBody: 'Pick one or more bouquets for the order.',
      stepTwoTitle: 'Delivery and greeting',
      stepTwoBody: 'Add address, delivery time, and your card message.',
      stepThreeTitle: 'Company and contact',
      stepThreeBody: 'Add contact details and choose payment method.',
      largeOrdersHeading: 'Need help?',
      cartHeading: 'Order overview',
      cartItemSingular: 'item',
      cartItemPlural: 'items',
      emptyState: 'Add a bouquet to see the order overview.',
      subtotalLabel: 'Subtotal excl. VAT',
      totalLabel: 'Total incl. VAT',
      deliveryHelp:
        'Choose a weekday and a time between 07:00-09:00 or 16:00-18:00.',
      submitLabel: 'Go to payment',
      submitLoading: 'Sending...',
      successHeading: 'Order received!',
      successBody: 'We will send a confirmation to your email shortly.',
      errorGeneric: 'An error occurred. Please try again.',
      noBouquetError: 'Please add at least one bouquet.',
      mobileCartLabel: 'View order',
    }
  : {
      stepOneTitle: 'Vælg blomster',
      stepOneBody: 'Vælg en eller flere buketter til bestillingen.',
      stepTwoTitle: 'Levering og hilsen',
      stepTwoBody: 'Tilføj adresse, leveringstid og korttekst.',
      stepThreeTitle: 'Virksomhed og kontakt',
      stepThreeBody: 'Tilføj kontaktoplysninger og vælg betaling.',
      largeOrdersHeading: 'Brug for hjælp?',
      cartHeading: 'Ordreoversigt',
      cartItemSingular: 'vare',
      cartItemPlural: 'varer',
      emptyState: 'Tilføj en buket for at se ordreoversigten.',
      subtotalLabel: 'Subtotal ekskl. moms',
      totalLabel: 'Total inkl. moms',
      deliveryHelp:
        'Vælg en hverdag og et tidspunkt mellem 07:00-09:00 eller 16:00-18:00.',
      submitLabel: 'Gå til betaling',
      submitLoading: 'Sender...',
      successHeading: 'Bestilling modtaget!',
      successBody: 'Vi sender en bekræftelse til din email snarest.',
      errorGeneric: 'Der opstod en fejl. Prøv venligst igen.',
      noBouquetError: 'Tilføj mindst en buket for at fortsætte.',
      mobileCartLabel: 'Se ordre',
    };
---

<Layout title={order.heading} locale={locale}>
  <Hero heading={order.heading} subheading={order.intro} compact />

  {(paymentSuccess || paymentCancelled) && (
    <div class="px-4 sm:px-6 lg:px-8">
      <div class="container-narrow mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div
          class:list={[
            'rounded-[1.5rem] px-5 py-4 text-sm shadow-ambient',
            paymentSuccess
              ? 'bg-primary-50 text-primary-700'
              : 'bg-secondary-100 text-secondary-700',
          ]}
        >
          <p class="font-semibold">
            {paymentSuccess ? order.form.successHeading : order.form.cancelledHeading}
          </p>
          <p class="mt-1">
            {paymentSuccess ? order.form.successBody : order.form.cancelledBody}
          </p>
        </div>
      </div>
    </div>
  )}

  <section class="px-4 pb-4 sm:px-6 lg:px-8">
    <div class="container-narrow mx-auto">
      <div class="rounded-[1.35rem] bg-surface-low px-5 py-4 text-sm text-surface-ink/72 shadow-ambient">
        <span><strong>{order.deliveryNoteLabel}</strong> {order.deliveryNote}</span>
      </div>
    </div>
  </section>

  <section class="section-padding pt-4">
    <div class="container-narrow mx-auto px-4 sm:px-6 lg:px-8">
      <div id="order-flow" class="grid gap-8 lg:grid-cols-[1.18fr,0.82fr] lg:items-start">
        <div class="space-y-6">
          <section class="surface-panel px-6 py-6 sm:px-8 sm:py-8">
            <div class="step-head">
              <span class="step-number">1</span>
              <div>
                <h2 class="font-heading text-3xl text-surface-ink sm:text-4xl">{uiText.stepOneTitle}</h2>
                <p class="mt-2 text-sm text-surface-ink/62">{uiText.stepOneBody}</p>
              </div>
            </div>

            <div class="mt-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
              {order.bouquets.map((bouquet: any) => (
                <div
                  class="bouquet-card surface-item relative overflow-hidden p-4 transition-all duration-300 sm:p-5"
                  data-id={bouquet.id}
                  data-name={bouquet.name}
                  data-price={bouquet.price}
                >
                  <div class="relative mb-4 flex aspect-[4/3] items-center justify-center overflow-hidden rounded-[1.35rem] bg-gradient-to-br from-secondary-100 via-surface-raised to-primary-100">
                    <span class="text-5xl opacity-30">💐</span>
                    <div class="qty-badge absolute right-3 top-3 hidden min-w-[28px] items-center justify-center rounded-full bg-primary-600 px-1.5 text-xs font-bold text-white shadow-ambient">
                      <span class="qty-badge-count">0</span>
                    </div>
                  </div>
                  <h3 class="font-heading text-2xl text-surface-ink">{bouquet.name}</h3>
                  <p class="mt-2 line-clamp-2 text-xs leading-relaxed text-surface-ink/58">{bouquet.description}</p>
                  <div class="mt-4 flex items-center justify-between gap-3">
                    <p class="text-sm font-semibold text-primary-600">
                      {bouquet.price},00
                      <span class="text-xs font-normal text-surface-ink/46">{order.priceLabel}</span>
                    </p>
                    <div class="flex items-center gap-1">
                      <button
                        type="button"
                        class="qty-dec hidden flex h-8 w-8 items-center justify-center rounded-full bg-surface-low text-surface-ink/60 transition-colors hover:bg-secondary-100 hover:text-secondary-600 focus:outline-none"
                        aria-label={isEnglish ? 'Remove one' : 'Fjern en'}
                      >
                        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M20 12H4"/></svg>
                      </button>
                      <span class="qty-display hidden w-8 text-center text-sm font-semibold text-surface-ink">0</span>
                      <button
                        type="button"
                        class="qty-inc flex h-8 w-8 items-center justify-center rounded-full bg-primary-600 text-white transition-colors hover:bg-primary-500 focus:outline-none"
                        aria-label={isEnglish ? 'Add one' : 'Tilføj en'}
                      >
                        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 4v16m8-8H4"/></svg>
                      </button>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </section>

          <form id="order-form" class="space-y-6">
            <section class="surface-panel px-6 py-6 sm:px-8 sm:py-8">
              <div class="step-head">
                <span class="step-number">2</span>
                <div>
                  <h2 class="font-heading text-3xl text-surface-ink sm:text-4xl">{uiText.stepTwoTitle}</h2>
                  <p class="mt-2 text-sm text-surface-ink/62">{uiText.stepTwoBody}</p>
                </div>
              </div>

              <div class="mt-6 grid gap-5 sm:grid-cols-2">
                <div class="sm:col-span-2">
                  <label class="label-text">{order.form.addressLabel} *</label>
                  <input type="text" name="address" required class="input-field !py-2.5" placeholder={isEnglish ? 'Street 12, 2630 Høje Taastrup' : 'Vejnavn 12, 2630 Høje Taastrup'} />
                </div>
                <div>
                  <label class="label-text">{isEnglish ? 'Delivery date' : 'Leveringsdato'} *</label>
                  <input type="date" name="deliveryDate" required class="input-field !py-2.5" />
                </div>
                <div>
                  <label class="label-text">{isEnglish ? 'Time' : 'Tidspunkt'} *</label>
                  <select name="deliveryClock" required class="input-field !py-2.5">
                    <option value="">{isEnglish ? 'Choose time' : 'Vælg tidspunkt'}</option>
                    {deliveryTimeOptions.map((time) => (
                      <option value={time}>{time}</option>
                    ))}
                  </select>
                </div>
                <div class="sm:col-span-2">
                  <p class="text-xs text-surface-ink/52">{uiText.deliveryHelp}</p>
                </div>
                <div class="sm:col-span-2">
                  <label class="label-text">{order.form.cardTextLabel}</label>
                  <input type="text" name="cardText" class="input-field !py-2.5" maxlength="250" placeholder={isEnglish ? 'E.g.: Happy Birthday!' : 'Fx: Tillykke med fødselsdagen!'} />
                </div>
                <div class="sm:col-span-2">
                  <label class="label-text">{order.form.commentLabel}</label>
                  <textarea name="comment" rows="3" class="input-field !py-2.5 resize-y"></textarea>
                </div>
              </div>
            </section>

            <section class="surface-panel px-6 py-6 sm:px-8 sm:py-8">
              <div class="step-head">
                <span class="step-number">3</span>
                <div>
                  <h2 class="font-heading text-3xl text-surface-ink sm:text-4xl">{uiText.stepThreeTitle}</h2>
                  <p class="mt-2 text-sm text-surface-ink/62">{uiText.stepThreeBody}</p>
                </div>
              </div>

              <div class="mt-6 grid gap-5 sm:grid-cols-2">
                <div>
                  <label class="label-text">{order.form.contactNameLabel || (isEnglish ? 'Contact person' : 'Kontaktperson')} *</label>
                  <input type="text" name="contactName" required class="input-field !py-2.5" placeholder={isEnglish ? 'Full name' : 'Fulde navn'} />
                </div>
                <div>
                  <label class="label-text">{order.form.companyNameLabel || (isEnglish ? 'Company name' : 'Virksomhedens navn')} *</label>
                  <input type="text" name="companyName" required class="input-field !py-2.5" placeholder={isEnglish ? 'Company name' : 'Virksomhedens navn'} />
                </div>
                <div>
                  <label class="label-text">{order.form.phoneLabel} *</label>
                  <input type="tel" name="phone" required class="input-field !py-2.5" placeholder="12345678" />
                </div>
                <div>
                  <label class="label-text">{order.form.emailLabel} *</label>
                  <input type="email" name="email" required class="input-field !py-2.5" placeholder={isEnglish ? 'your@company.com' : 'din@firma.dk'} />
                </div>
                <div>
                  <label class="label-text">{order.form.companyCodeLabel}</label>
                  <input type="text" name="companyCode" class="input-field !py-2.5" />
                </div>
                <div id="invoice-email-field" class="hidden">
                  <label class="label-text">{order.form.invoiceEmailLabel}</label>
                  <input type="email" name="invoiceEmail" class="input-field !py-2.5" placeholder={isEnglish ? 'invoice@company.com' : 'faktura@firma.dk'} />
                </div>
                <div class="sm:col-span-2">
                  <label class="label-text">{order.form.paymentLabel} *</label>
                  <div class="mt-2 grid gap-3">
                    {order.form.paymentOptions.map((option: any) => (
                      <label class="payment-option flex cursor-pointer items-center gap-3 rounded-[1rem] bg-surface-raised px-4 py-3 text-sm text-surface-ink/76 transition-colors hover:bg-white">
                        <input type="radio" name="payment" value={option.id} required class="h-3.5 w-3.5 text-primary-600 border-gray-300 focus:ring-primary-500" />
                        <span class="flex-1">{option.label}</span>
                      </label>
                    ))}
                  </div>
                </div>
              </div>
            </section>
          </form>
        </div>

        <aside class="space-y-4 lg:sticky lg:top-24">
          <div id="cart-panel" class="surface-panel overflow-hidden px-6 py-6">
            <div>
              <h3 class="font-heading text-3xl text-surface-ink">{uiText.cartHeading}</h3>
              <span id="cart-item-count" class="mt-2 block text-sm text-surface-ink/54">0 {uiText.cartItemPlural}</span>
            </div>

            <div id="cart-empty" class="py-10 text-center text-surface-ink/38">
              <svg class="mx-auto mb-3 w-10 h-10 opacity-30" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 3h2l.4 2M7 13h10l4-9H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
              <p class="text-sm">{uiText.emptyState}</p>
            </div>

            <div id="cart-items" class="hidden">
              <div id="cart-list" class="space-y-2.5"></div>

              <div class="mt-5 rounded-[1.25rem] bg-surface-raised p-4">
                <div class="mb-1 flex justify-between text-sm text-surface-ink/56">
                  <span>{uiText.subtotalLabel}</span>
                  <span id="cart-subtotal">0 kr</span>
                </div>
                <div class="flex justify-between font-semibold text-surface-ink">
                  <span>{uiText.totalLabel}</span>
                  <span id="cart-total">0 kr</span>
                </div>
              </div>

              <button type="submit" form="order-form" id="checkout-btn" class="btn-primary mt-5 w-full !py-3.5 !text-sm flex items-center justify-center gap-2">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                </svg>
                {uiText.submitLabel}
              </button>
              <p class="mt-3 text-center text-xs text-surface-ink/42">{order.form.confirmationNote}</p>
            </div>
          </div>

          <div class="surface-panel p-5 text-sm text-surface-ink/72">
            <p class="mb-1 font-medium text-primary-600">{uiText.largeOrdersHeading}</p>
            <p>{order.contactNote} <a href={`mailto:${order.contactEmail}`} class="break-all underline hover:no-underline sm:break-words [overflow-wrap:anywhere]">{order.contactEmail}</a> {isEnglish ? 'or' : 'eller'} <a href={`tel:${order.contactPhone}`} class="underline hover:no-underline">{order.contactPhone}</a></p>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <div id="mobile-cart-bar" class="hidden lg:hidden fixed bottom-0 inset-x-0 z-50 bg-surface-raised shadow-ambient px-4 py-3">
    <button id="mobile-cart-open" class="btn-primary w-full !py-3 flex items-center justify-between">
      <span class="flex items-center gap-2">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-9H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
        </svg>
        <span>{uiText.mobileCartLabel}</span>
      </span>
      <span id="mobile-cart-total-label" class="font-semibold">0 kr</span>
    </button>
  </div>
</Layout>

<style>
  .surface-item {
    background: #ffffff;
    border-radius: 1.5rem;
    box-shadow: 0 12px 40px rgba(26, 28, 25, 0.06);
  }

  .step-head {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .step-number {
    display: inline-flex;
    width: 2.2rem;
    height: 2.2rem;
    align-items: center;
    justify-content: center;
    border-radius: 9999px;
    background: #526349;
    color: #ffffff;
    font-size: 0.9rem;
    font-weight: 700;
    flex-shrink: 0;
  }

  .bouquet-card.has-items {
    box-shadow: 0 18px 44px rgba(26, 28, 25, 0.1);
    transform: translateY(-2px);
  }

  .bouquet-card.has-items .qty-badge {
    display: flex;
  }

  .bouquet-card.has-items .qty-inc {
    background-image: linear-gradient(135deg, #526349 0%, #90a384 100%);
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>

<script define:vars={{ locale, uiText, paymentSuccess }}>
  const cart = new Map();
  const CART_STORAGE_KEY = `boulevardens-blomster-cart-${locale}`;

  function getTodayDateValue() {
    const now = new Date();
    const localNow = new Date(now.getTime() - now.getTimezoneOffset() * 60000);
    return localNow.toISOString().split('T')[0];
  }

  function saveCartState() {
    try {
      const snapshot = Array.from(cart.values()).map((item) => ({
        id: item.id,
        qty: item.qty,
      }));

      if (snapshot.length === 0) {
        localStorage.removeItem(CART_STORAGE_KEY);
        return;
      }

      localStorage.setItem(CART_STORAGE_KEY, JSON.stringify(snapshot));
    } catch (error) {
      console.warn('Could not save cart state.', error);
    }
  }

  function clearCartState() {
    try {
      localStorage.removeItem(CART_STORAGE_KEY);
    } catch (error) {
      console.warn('Could not clear cart state.', error);
    }
  }

  function setQty(id, name, price, delta) {
    const current = cart.get(id);
    const nextQty = (current?.qty ?? 0) + delta;

    if (nextQty <= 0) {
      cart.delete(id);
      saveCartState();
      return;
    }

    cart.set(id, { id, name, price, qty: nextQty });
    saveCartState();
  }

  function formatPrice(kr) {
    return `${new Intl.NumberFormat('da-DK', {
      minimumFractionDigits: 2,
      maximumFractionDigits: 2,
    }).format(kr)} kr`;
  }

  function syncCardUI(id) {
    const qty = cart.get(id)?.qty ?? 0;
    const card = document.querySelector(`.bouquet-card[data-id="${id}"]`);

    if (!card) return;

    const dec = card.querySelector('.qty-dec');
    const display = card.querySelector('.qty-display');
    const badge = card.querySelector('.qty-badge');
    const badgeCount = card.querySelector('.qty-badge-count');

    dec?.classList.toggle('hidden', qty === 0);
    display?.classList.toggle('hidden', qty === 0);

    if (display) display.textContent = String(qty);

    card.classList.toggle('has-items', qty > 0);
    badge?.classList.toggle('hidden', qty === 0);

    if (badgeCount) badgeCount.textContent = String(qty);
  }

  function renderCart() {
    const items = Array.from(cart.values());
    const totalQty = items.reduce((sum, item) => sum + item.qty, 0);
    const subtotal = items.reduce((sum, item) => sum + item.price * item.qty, 0);
    const total = Math.round(subtotal * 125) / 100;
    const hasItems = totalQty > 0;

    document.getElementById('cart-empty')?.classList.toggle('hidden', hasItems);
    document.getElementById('cart-items')?.classList.toggle('hidden', !hasItems);
    document.getElementById('mobile-cart-bar')?.classList.toggle('hidden', !hasItems);

    const countLabel = `${totalQty} ${totalQty === 1 ? uiText.cartItemSingular : uiText.cartItemPlural}`;
    const itemCount = document.getElementById('cart-item-count');
    const subtotalElement = document.getElementById('cart-subtotal');
    const totalElement = document.getElementById('cart-total');
    const mobileTotal = document.getElementById('mobile-cart-total-label');

    if (itemCount) itemCount.textContent = countLabel;
    if (subtotalElement) subtotalElement.textContent = formatPrice(subtotal);
    if (totalElement) totalElement.textContent = formatPrice(total);
    if (mobileTotal) mobileTotal.textContent = formatPrice(total);

    const cartList = document.getElementById('cart-list');

    if (!cartList) return;

    if (!hasItems) {
      cartList.innerHTML = '';
      return;
    }

    cartList.innerHTML = items
      .map(
        (item) => `
          <div class="flex items-center justify-between gap-2 rounded-[1rem] bg-surface-raised px-3 py-2 text-sm">
            <div class="min-w-0 flex-1">
              <p class="truncate font-medium text-surface-ink">${item.name}</p>
              <p class="text-xs text-surface-ink/44">${item.qty} × ${formatPrice(item.price)}</p>
            </div>
            <div class="flex flex-shrink-0 items-center gap-1">
              <button type="button" class="cart-dec flex h-6 w-6 items-center justify-center rounded-full bg-surface-low text-surface-ink/50 transition-colors hover:bg-secondary-100 hover:text-secondary-600" data-id="${item.id}">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M20 12H4"/></svg>
              </button>
              <span class="w-5 text-center text-xs font-semibold text-surface-ink">${item.qty}</span>
              <button type="button" class="cart-inc flex h-6 w-6 items-center justify-center rounded-full bg-surface-low text-surface-ink/50 transition-colors hover:bg-primary-100 hover:text-primary-600" data-id="${item.id}">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 4v16m8-8H4"/></svg>
              </button>
              <span class="ml-1 w-16 text-right text-xs font-semibold text-surface-ink/72">${formatPrice(item.price * item.qty)}</span>
            </div>
          </div>
        `,
      )
      .join('');

    cartList.querySelectorAll('.cart-dec').forEach((button) => {
      button.addEventListener('click', () => {
        const id = button.dataset.id;
        const item = id ? cart.get(id) : null;

        if (!id || !item) return;

        setQty(id, item.name, item.price, -1);
        syncCardUI(id);
        renderCart();
      });
    });

    cartList.querySelectorAll('.cart-inc').forEach((button) => {
      button.addEventListener('click', () => {
        const id = button.dataset.id;
        const item = id ? cart.get(id) : null;

        if (!id || !item) return;

        setQty(id, item.name, item.price, 1);
        syncCardUI(id);
        renderCart();
      });
    });
  }

  document.querySelectorAll('.bouquet-card').forEach((card) => {
    const id = card.dataset.id;
    const name = card.dataset.name;
    const price = Number(card.dataset.price);

    if (!id || !name || Number.isNaN(price)) return;

    card.querySelector('.qty-inc')?.addEventListener('click', (event) => {
      event.stopPropagation();
      setQty(id, name, price, 1);
      syncCardUI(id);
      renderCart();
    });

    card.querySelector('.qty-dec')?.addEventListener('click', (event) => {
      event.stopPropagation();
      setQty(id, name, price, -1);
      syncCardUI(id);
      renderCart();
    });
  });

  if (paymentSuccess) {
    clearCartState();
  } else {
    try {
      const rawCartState = localStorage.getItem(CART_STORAGE_KEY);
      const storedItems = rawCartState ? JSON.parse(rawCartState) : [];

      storedItems.forEach((storedItem) => {
        if (!storedItem || typeof storedItem.id !== 'string') {
          return;
        }

        const qty = Number(storedItem.qty);
        const card = document.querySelector(`.bouquet-card[data-id="${storedItem.id}"]`);
        const name = card?.dataset.name;
        const price = Number(card?.dataset.price);

        if (!card || !name || !Number.isInteger(qty) || qty <= 0 || Number.isNaN(price)) {
          return;
        }

        cart.set(storedItem.id, {
          id: storedItem.id,
          name,
          price,
          qty,
        });
        syncCardUI(storedItem.id);
      });

      renderCart();
    } catch (error) {
      console.warn('Could not restore cart state.', error);
      clearCartState();
    }
  }

  document.querySelectorAll('input[name="payment"]').forEach((input) => {
    input.addEventListener('change', () => {
      const value = input.value;
      document.getElementById('invoice-email-field')?.classList.toggle('hidden', value !== 'faktura');
    });
  });

  const form = document.getElementById('order-form');
  const checkoutButton = document.getElementById('checkout-btn');
  const deliveryDateInput = document.querySelector('input[name="deliveryDate"]');

  if (deliveryDateInput) {
    deliveryDateInput.min = getTodayDateValue();
  }

  form?.addEventListener('submit', async (event) => {
    event.preventDefault();

    const items = Array.from(cart.values());

    if (items.length === 0) {
      alert(uiText.noBouquetError);
      return;
    }

    if (checkoutButton) {
      checkoutButton.disabled = true;
      checkoutButton.innerHTML = `<svg class="animate-spin w-4 h-4" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8z"></path></svg> ${uiText.submitLoading}`;
    }

    try {
      const formData = new FormData(form);
      const deliveryDate = String(formData.get('deliveryDate') || '');
      const deliveryClock = String(formData.get('deliveryClock') || '');
      const selectedDate = deliveryDate ? new Date(`${deliveryDate}T12:00:00`) : null;
      const selectedDay = selectedDate ? selectedDate.getDay() : null;

      if (selectedDay === 0 || selectedDay === 6) {
        throw new Error(
          locale === 'en'
            ? 'Please choose a weekday for delivery.'
            : 'Vælg venligst en hverdag til levering.',
        );
      }

        const payload = {
          address: String(formData.get('address') || ''),
          deliveryDate,
          deliveryClock,
        deliveryTime:
          deliveryDate && deliveryClock
            ? locale === 'en'
              ? `${deliveryDate} at ${deliveryClock}`
              : `${deliveryDate} kl. ${deliveryClock}`
            : '',
          cardText: String(formData.get('cardText') || ''),
          contactName: String(formData.get('contactName') || ''),
          companyName: String(formData.get('companyName') || ''),
          phone: String(formData.get('phone') || ''),
          email: String(formData.get('email') || ''),
        companyCode: String(formData.get('companyCode') || ''),
        comment: String(formData.get('comment') || ''),
        payment: String(formData.get('payment') || ''),
        invoiceEmail: String(formData.get('invoiceEmail') || ''),
        locale,
        lineItems: items.map((item) => ({
          id: item.id,
          qty: item.qty,
        })),
      };

      const response = await fetch('/api/checkout', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      });
      const result = await response.json();

      if (!response.ok) {
        throw new Error(result.error || uiText.errorGeneric);
      }

      if (result.url) {
        window.location.href = result.url;
        return;
      }

      if (result.success) {
        clearCartState();
        cart.clear();

        const orderFlow = document.getElementById('order-flow');

        if (orderFlow) {
          orderFlow.innerHTML = `
            <div class="lg:col-span-2">
              <div class="surface-panel p-10 text-center">
                <div class="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-primary-100">
                  <svg class="w-8 h-8 text-primary-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
                <h3 class="mb-2 font-heading text-2xl font-bold text-surface-ink">${uiText.successHeading}</h3>
                <p class="text-sm text-surface-ink/58">${uiText.successBody}</p>
              </div>
            </div>
          `;
        }

        document.getElementById('mobile-cart-bar')?.classList.add('hidden');
        return;
      }

      throw new Error(result.error || uiText.errorGeneric);
    } catch (error) {
      alert(error instanceof Error && error.message ? error.message : uiText.errorGeneric);

      if (checkoutButton) {
        checkoutButton.disabled = false;
        checkoutButton.innerHTML = `<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg> ${uiText.submitLabel}`;
      }
    }
  });

  document.getElementById('mobile-cart-open')?.addEventListener('click', () => {
    document.getElementById('cart-panel')?.scrollIntoView({
      behavior: 'smooth',
      block: 'start',
    });
  });
</script>
